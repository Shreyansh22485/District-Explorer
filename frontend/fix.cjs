const fs = require("fs"); let c = fs.readFileSync("d:/BTP/district-explorer/backend/src/server.js", "utf8"); c = c.replace("Our Machine Learning model (targeting \"${configModel}\") evaluated this on a median village profile.", "Our Machine Learning model (targeting \"${configModel}\") evaluated this on ${district && village ? `the village of ${village} in ${district} district` : `a typical rural village`}."); c = c.replace("The user proposed this scenario: \"${query}\"", "The user proposed this scenario for ${district && village ? `the village of ${village} in ${district} district` : `a typical rural village`}: \"${query}\""); c = c.replace("interpreting these results.", "interpreting these results specifically for ${district && village ? `the village of ${village} in ${district} district` : `a typical rural village`}."); fs.writeFileSync("d:/BTP/district-explorer/backend/src/server.js", c);
